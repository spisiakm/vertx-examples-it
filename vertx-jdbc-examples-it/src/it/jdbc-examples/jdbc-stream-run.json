{
  "name": "jdbc-stream",

  "tags": ["jdbc", "client", "stream"],

  "libraries": [
    "${dependencies}/postgresql"
  ],

  "executions": {

    "java" : {
      "directory" : "${base}",
      "command" : "java -jar ${jarFile} run io.vertx.example.jdbc.streaming.JDBCExample -cp ${base}/target/classes:."
    }

  },

  "grace-text": "A postgres database has been stopped successfully.",
  "post-check": "jdbc-stream-check.groovy"
}