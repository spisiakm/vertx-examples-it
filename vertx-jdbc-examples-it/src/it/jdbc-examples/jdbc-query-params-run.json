{
  "name": "jdbc-query-params",

  "tags": ["jdbc", "client", "query", "params"],

  "libraries": [
    "${dependencies}/postgresql"
  ],

  "executions": {

    "java" : {
      "directory" : "${base}",
      "command" : "java -jar ${jarFile} run io.vertx.example.jdbc.query_params.JDBCExample -cp ${base}/target/classes:."
    }

  },

  "grace-text": "A postgres database has been stopped successfully.",
  "post-check": "jdbc-query-params-check.groovy"
}