{
  "name": "jdbc-simple",

  "tags": ["jdbc", "client", "simple"],

  "libraries": [
    "${dependencies}/postgresql"
  ],

  "executions": {

    "java" : {
      "directory" : "${base}",
      "command" : "java -jar ${jarFile} run io.vertx.example.jdbc.simple.JDBCExample -cp ${base}/target/classes:."
    }

  },

  "grace-text": "A postgres database has been stopped successfully.",
  "post-check": "jdbc-simple-check.groovy"
}