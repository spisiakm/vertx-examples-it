{
  "name": "jdbc-transaction-rollback",

  "tags": ["jdbc", "client", "transaction", "rollback"],

  "libraries": [
    "${dependencies}/postgresql"
  ],

  "executions": {

    "java" : {
      "directory" : "${base}",
      "command" : "java -jar ${jarFile} run io.vertx.example.jdbc.transaction_rollback.JDBCExample -cp ${base}/target/classes:."
    }

  },

  "grace-text": "A postgres database has been stopped successfully.",
  "post-check": "jdbc-transaction-rollback-check.groovy"
}