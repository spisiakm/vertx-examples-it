{
  "name": "web-auth-jdbc",

  "tags": ["web", "auth", "jdbc"],

  "libraries": [
    "${dependencies}"
  ],

  "executions": {

    "java" : {
      "directory" : "${base}/target/classes",
      "command" : "java -jar ${jarFile} run io.vertx.example.web.authjdbc.Server"
    }

  },

  "grace-text": "Succeeded in deploying verticle",
  "client-check": "web-auth-check.groovy"
}